import { ShapeHostDirective } from './shape-host.directive';

describe('ShapeHostDirective', () => {
  it('should create an instance', () => {
    const directive = new ShapeHostDirective();
    expect(directive).toBeTruthy();
  });
});
